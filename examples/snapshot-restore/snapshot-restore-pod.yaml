apiVersion: v1
kind: Pod
metadata:
  name: snapshot-restore-pod
  namespace: default
spec:
  containers:
  - name: client-container
    image: nginx
    volumeMounts:
    - name: snapshot-restore-pvc
      mountPath: /data
      readOnly: false
  volumes:
  - name: snapshot-restore-pvc
    persistentVolumeClaim:
      claimName: snapshot-restore-pvc
